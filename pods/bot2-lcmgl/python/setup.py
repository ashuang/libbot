#!/usr/bin/env python

import os
from distutils.core import setup
from distutils.debug import DEBUG

setup(name='LCMGL',
        version='0.0.1',
        description='LCMGL',
        packages = [ 'lcmgl' ],
        )

